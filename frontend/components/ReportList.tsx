"use client";
import type { Report } from "../lib/types";
import styles from "./ReportList.module.css";
export function ReportList({ reports, onUpvote }: { reports: Report[]; onUpvote?: (id:number)=>void }) { if(!reports.length) return <div className="card empty">No approved reports match these filters yet.</div>; return <div className={styles.list}>{reports.map(report => <article className={`card ${styles.report}`} key={report.id}><div className={styles.top}><span className={styles.category}>{report.category}</span><time>{new Date(report.incident_at || report.created_at).toLocaleDateString()}</time></div><h3>{report.title}</h3><p className={styles.location}>⌖ {report.approximate_location_name}</p><p>{report.description}</p><div className={styles.bottom}><span>{report.upvote_count} community support</span>{onUpvote && <button className="button secondary" onClick={()=>onUpvote(report.id)} aria-label={`Upvote ${report.title}`}>▲ Upvote</button>}</div></article>)}</div>; }
